// @ynara/ui — fundaciones de UI compartibles web/mobile (ver README).
// Barrel público del package. Vacío a propósito: el wiring ya está
// verificado (tsc · next build · vitest) y listo para poblarse con el
// set de íconos (F0.2) y el sistema gráfico "Red de memoria" (F0.3).
export {};
