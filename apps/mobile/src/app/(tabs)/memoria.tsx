import { MemoriaScreen } from "@/features/memory/MemoriaScreen";

// Tab "Memoria" (ruta `/memoria`): timeline de recuerdos de las 3 capas.
export default function MemoriaTab() {
  return <MemoriaScreen />;
}
