name: Deploy Web

on:
  workflow_dispatch:

# El deploy real de apps/web lo gestiona Vercel via Git integration.
# Este workflow queda solo como slot manual para tareas auxiliares
# post-deploy: smoke tests, purge cache de Cloudflare, notificacion a
# Slack, etc. Se dispara a mano desde la pestana Actions.

jobs:
  notify:
    name: Notificar deploy
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "TODO integrar notificacion de deploy de Vercel"
